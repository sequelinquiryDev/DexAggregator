## Packages
framer-motion | Smooth animations for table rows and layout transitions
lucide-react | Beautiful icons for the dashboard interface
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
API endpoints follow the pattern /api/snapshot/:chain
Supports 'ethereum' and 'polygon' chains initially
Auto-refresh required every 10 seconds
